// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "simple_rpg";

/** Server → Client: Session initialization */
export interface InitMessage {
  playerId: number;
  entities: { [key: number]: EntityInit };
  tileRegistry: { [key: number]: string };
}

export interface InitMessage_EntitiesEntry {
  key: number;
  value: EntityInit | undefined;
}

export interface InitMessage_TileRegistryEntry {
  key: number;
  value: string;
}

export interface EntityInit {
  x: number;
  y: number;
  type: string;
  focusedId: number;
}

/** Server → Client: Inventory contents (on-demand) */
export interface InventoryContents {
  items: ItemSlot[];
  currentVolume: number;
  maxVolume: number;
  currentWeight: number;
}

export interface ItemSlot {
  name: string;
  spriteKey: string;
  volume: number;
  weight: number;
  stackable: boolean;
  quantity: number;
  maxStack: number;
}

/** Server → Client: Interaction response (loot UI, dialog) */
export interface InteractionResponse {
  targetId: number;
  interactionType: string;
  playerInventory: InventoryContents | undefined;
  targetInventory: InventoryContents | undefined;
}

function createBaseInitMessage(): InitMessage {
  return { playerId: 0, entities: {}, tileRegistry: {} };
}

export const InitMessage: MessageFns<InitMessage> = {
  encode(message: InitMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== 0) {
      writer.uint32(8).uint32(message.playerId);
    }
    globalThis.Object.entries(message.entities).forEach(([key, value]: [string, EntityInit]) => {
      InitMessage_EntitiesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    globalThis.Object.entries(message.tileRegistry).forEach(([key, value]: [string, string]) => {
      InitMessage_TileRegistryEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.playerId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = InitMessage_EntitiesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.entities[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = InitMessage_TileRegistryEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.tileRegistry[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitMessage {
    return {
      playerId: isSet(object.playerId)
        ? globalThis.Number(object.playerId)
        : isSet(object.player_id)
        ? globalThis.Number(object.player_id)
        : 0,
      entities: isObject(object.entities)
        ? (globalThis.Object.entries(object.entities) as [string, any][]).reduce(
          (acc: { [key: number]: EntityInit }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = EntityInit.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      tileRegistry: isObject(object.tileRegistry)
        ? (globalThis.Object.entries(object.tileRegistry) as [string, any][]).reduce(
          (acc: { [key: number]: string }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.tile_registry)
        ? (globalThis.Object.entries(object.tile_registry) as [string, any][]).reduce(
          (acc: { [key: number]: string }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: InitMessage): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.entities) {
      const entries = globalThis.Object.entries(message.entities) as [string, EntityInit][];
      if (entries.length > 0) {
        obj.entities = {};
        entries.forEach(([k, v]) => {
          obj.entities[k] = EntityInit.toJSON(v);
        });
      }
    }
    if (message.tileRegistry) {
      const entries = globalThis.Object.entries(message.tileRegistry) as [string, string][];
      if (entries.length > 0) {
        obj.tileRegistry = {};
        entries.forEach(([k, v]) => {
          obj.tileRegistry[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitMessage>, I>>(base?: I): InitMessage {
    return InitMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitMessage>, I>>(object: I): InitMessage {
    const message = createBaseInitMessage();
    message.playerId = object.playerId ?? 0;
    message.entities = (globalThis.Object.entries(object.entities ?? {}) as [string, EntityInit][]).reduce(
      (acc: { [key: number]: EntityInit }, [key, value]: [string, EntityInit]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = EntityInit.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.tileRegistry = (globalThis.Object.entries(object.tileRegistry ?? {}) as [string, string][]).reduce(
      (acc: { [key: number]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseInitMessage_EntitiesEntry(): InitMessage_EntitiesEntry {
  return { key: 0, value: undefined };
}

export const InitMessage_EntitiesEntry: MessageFns<InitMessage_EntitiesEntry> = {
  encode(message: InitMessage_EntitiesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value !== undefined) {
      EntityInit.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitMessage_EntitiesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitMessage_EntitiesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = EntityInit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitMessage_EntitiesEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? EntityInit.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: InitMessage_EntitiesEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== undefined) {
      obj.value = EntityInit.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitMessage_EntitiesEntry>, I>>(base?: I): InitMessage_EntitiesEntry {
    return InitMessage_EntitiesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitMessage_EntitiesEntry>, I>>(object: I): InitMessage_EntitiesEntry {
    const message = createBaseInitMessage_EntitiesEntry();
    message.key = object.key ?? 0;
    message.value = (object.value !== undefined && object.value !== null)
      ? EntityInit.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseInitMessage_TileRegistryEntry(): InitMessage_TileRegistryEntry {
  return { key: 0, value: "" };
}

export const InitMessage_TileRegistryEntry: MessageFns<InitMessage_TileRegistryEntry> = {
  encode(message: InitMessage_TileRegistryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitMessage_TileRegistryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitMessage_TileRegistryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitMessage_TileRegistryEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: InitMessage_TileRegistryEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitMessage_TileRegistryEntry>, I>>(base?: I): InitMessage_TileRegistryEntry {
    return InitMessage_TileRegistryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitMessage_TileRegistryEntry>, I>>(
    object: I,
  ): InitMessage_TileRegistryEntry {
    const message = createBaseInitMessage_TileRegistryEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEntityInit(): EntityInit {
  return { x: 0, y: 0, type: "", focusedId: 0 };
}

export const EntityInit: MessageFns<EntityInit> = {
  encode(message: EntityInit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(13).float(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(21).float(message.y);
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.focusedId !== 0) {
      writer.uint32(32).uint32(message.focusedId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EntityInit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEntityInit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.x = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.y = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.focusedId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EntityInit {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      focusedId: isSet(object.focusedId)
        ? globalThis.Number(object.focusedId)
        : isSet(object.focused_id)
        ? globalThis.Number(object.focused_id)
        : 0,
    };
  },

  toJSON(message: EntityInit): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.focusedId !== 0) {
      obj.focusedId = Math.round(message.focusedId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EntityInit>, I>>(base?: I): EntityInit {
    return EntityInit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EntityInit>, I>>(object: I): EntityInit {
    const message = createBaseEntityInit();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.type = object.type ?? "";
    message.focusedId = object.focusedId ?? 0;
    return message;
  },
};

function createBaseInventoryContents(): InventoryContents {
  return { items: [], currentVolume: 0, maxVolume: 0, currentWeight: 0 };
}

export const InventoryContents: MessageFns<InventoryContents> = {
  encode(message: InventoryContents, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ItemSlot.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.currentVolume !== 0) {
      writer.uint32(21).float(message.currentVolume);
    }
    if (message.maxVolume !== 0) {
      writer.uint32(29).float(message.maxVolume);
    }
    if (message.currentWeight !== 0) {
      writer.uint32(37).float(message.currentWeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InventoryContents {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInventoryContents();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ItemSlot.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.currentVolume = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.maxVolume = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.currentWeight = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InventoryContents {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ItemSlot.fromJSON(e)) : [],
      currentVolume: isSet(object.currentVolume)
        ? globalThis.Number(object.currentVolume)
        : isSet(object.current_volume)
        ? globalThis.Number(object.current_volume)
        : 0,
      maxVolume: isSet(object.maxVolume)
        ? globalThis.Number(object.maxVolume)
        : isSet(object.max_volume)
        ? globalThis.Number(object.max_volume)
        : 0,
      currentWeight: isSet(object.currentWeight)
        ? globalThis.Number(object.currentWeight)
        : isSet(object.current_weight)
        ? globalThis.Number(object.current_weight)
        : 0,
    };
  },

  toJSON(message: InventoryContents): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ItemSlot.toJSON(e));
    }
    if (message.currentVolume !== 0) {
      obj.currentVolume = message.currentVolume;
    }
    if (message.maxVolume !== 0) {
      obj.maxVolume = message.maxVolume;
    }
    if (message.currentWeight !== 0) {
      obj.currentWeight = message.currentWeight;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InventoryContents>, I>>(base?: I): InventoryContents {
    return InventoryContents.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InventoryContents>, I>>(object: I): InventoryContents {
    const message = createBaseInventoryContents();
    message.items = object.items?.map((e) => ItemSlot.fromPartial(e)) || [];
    message.currentVolume = object.currentVolume ?? 0;
    message.maxVolume = object.maxVolume ?? 0;
    message.currentWeight = object.currentWeight ?? 0;
    return message;
  },
};

function createBaseItemSlot(): ItemSlot {
  return { name: "", spriteKey: "", volume: 0, weight: 0, stackable: false, quantity: 0, maxStack: 0 };
}

export const ItemSlot: MessageFns<ItemSlot> = {
  encode(message: ItemSlot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.spriteKey !== "") {
      writer.uint32(18).string(message.spriteKey);
    }
    if (message.volume !== 0) {
      writer.uint32(29).float(message.volume);
    }
    if (message.weight !== 0) {
      writer.uint32(37).float(message.weight);
    }
    if (message.stackable !== false) {
      writer.uint32(40).bool(message.stackable);
    }
    if (message.quantity !== 0) {
      writer.uint32(48).int32(message.quantity);
    }
    if (message.maxStack !== 0) {
      writer.uint32(56).int32(message.maxStack);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemSlot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseItemSlot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.spriteKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.volume = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.weight = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.stackable = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.quantity = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxStack = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ItemSlot {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      spriteKey: isSet(object.spriteKey)
        ? globalThis.String(object.spriteKey)
        : isSet(object.sprite_key)
        ? globalThis.String(object.sprite_key)
        : "",
      volume: isSet(object.volume) ? globalThis.Number(object.volume) : 0,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      stackable: isSet(object.stackable) ? globalThis.Boolean(object.stackable) : false,
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      maxStack: isSet(object.maxStack)
        ? globalThis.Number(object.maxStack)
        : isSet(object.max_stack)
        ? globalThis.Number(object.max_stack)
        : 0,
    };
  },

  toJSON(message: ItemSlot): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.spriteKey !== "") {
      obj.spriteKey = message.spriteKey;
    }
    if (message.volume !== 0) {
      obj.volume = message.volume;
    }
    if (message.weight !== 0) {
      obj.weight = message.weight;
    }
    if (message.stackable !== false) {
      obj.stackable = message.stackable;
    }
    if (message.quantity !== 0) {
      obj.quantity = Math.round(message.quantity);
    }
    if (message.maxStack !== 0) {
      obj.maxStack = Math.round(message.maxStack);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemSlot>, I>>(base?: I): ItemSlot {
    return ItemSlot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemSlot>, I>>(object: I): ItemSlot {
    const message = createBaseItemSlot();
    message.name = object.name ?? "";
    message.spriteKey = object.spriteKey ?? "";
    message.volume = object.volume ?? 0;
    message.weight = object.weight ?? 0;
    message.stackable = object.stackable ?? false;
    message.quantity = object.quantity ?? 0;
    message.maxStack = object.maxStack ?? 0;
    return message;
  },
};

function createBaseInteractionResponse(): InteractionResponse {
  return { targetId: 0, interactionType: "", playerInventory: undefined, targetInventory: undefined };
}

export const InteractionResponse: MessageFns<InteractionResponse> = {
  encode(message: InteractionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.targetId !== 0) {
      writer.uint32(8).uint32(message.targetId);
    }
    if (message.interactionType !== "") {
      writer.uint32(18).string(message.interactionType);
    }
    if (message.playerInventory !== undefined) {
      InventoryContents.encode(message.playerInventory, writer.uint32(26).fork()).join();
    }
    if (message.targetInventory !== undefined) {
      InventoryContents.encode(message.targetInventory, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InteractionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInteractionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.targetId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.interactionType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.playerInventory = InventoryContents.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.targetInventory = InventoryContents.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InteractionResponse {
    return {
      targetId: isSet(object.targetId)
        ? globalThis.Number(object.targetId)
        : isSet(object.target_id)
        ? globalThis.Number(object.target_id)
        : 0,
      interactionType: isSet(object.interactionType)
        ? globalThis.String(object.interactionType)
        : isSet(object.interaction_type)
        ? globalThis.String(object.interaction_type)
        : "",
      playerInventory: isSet(object.playerInventory)
        ? InventoryContents.fromJSON(object.playerInventory)
        : isSet(object.player_inventory)
        ? InventoryContents.fromJSON(object.player_inventory)
        : undefined,
      targetInventory: isSet(object.targetInventory)
        ? InventoryContents.fromJSON(object.targetInventory)
        : isSet(object.target_inventory)
        ? InventoryContents.fromJSON(object.target_inventory)
        : undefined,
    };
  },

  toJSON(message: InteractionResponse): unknown {
    const obj: any = {};
    if (message.targetId !== 0) {
      obj.targetId = Math.round(message.targetId);
    }
    if (message.interactionType !== "") {
      obj.interactionType = message.interactionType;
    }
    if (message.playerInventory !== undefined) {
      obj.playerInventory = InventoryContents.toJSON(message.playerInventory);
    }
    if (message.targetInventory !== undefined) {
      obj.targetInventory = InventoryContents.toJSON(message.targetInventory);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InteractionResponse>, I>>(base?: I): InteractionResponse {
    return InteractionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InteractionResponse>, I>>(object: I): InteractionResponse {
    const message = createBaseInteractionResponse();
    message.targetId = object.targetId ?? 0;
    message.interactionType = object.interactionType ?? "";
    message.playerInventory = (object.playerInventory !== undefined && object.playerInventory !== null)
      ? InventoryContents.fromPartial(object.playerInventory)
      : undefined;
    message.targetInventory = (object.targetInventory !== undefined && object.targetInventory !== null)
      ? InventoryContents.fromPartial(object.targetInventory)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
